/*

This program explain that how to define object like macro using #define directive.
This program expalin that we can use any C keyword in macros

Usage	: #define MACRO_NAME macro_expansion

*/

#include <stdio.h>

#define INTEGER int	//object like macro for a c keyword 'int'
#define CONDITION if	//object like macro for a c keyword 'if'

int main()
{
	INTEGER a=30,b=20;
	CONDITION(a>b)
		printf("A is Big\n");
	else
		printf("B is Big\n");

}

